<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="master.if26.vote"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="19" />
		
    <!-- Autoriser l'application à utiliser internet. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <!-- Première activity, lancée au démarrage de l'application -->
        <activity
            android:name=".Activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="master.if26.vote.Activity.MenuActivity"
            android:label="@string/title_activity_MenuActivity" >
        </activity>

        <activity
            android:name="master.if26.vote.Activity.CandidatsActivity"
            android:label="@string/title_activity_Candidats" >
        </activity>

        <!-- Déclaration de l'activity de création de compte -->
        <activity
            android:name="master.if26.vote.Activity.CreateAccountActivity"
            android:label="@string/title_activity_CreateAccount" >
        </activity>

        <!-- -->
        <activity
            android:name="master.if26.vote.Activity.ResultatsActivity"
            android:label="@string/title_activity_Resultats" >
        </activity>

    </application>

</manifest>
